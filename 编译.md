# Windows 编译流程

本文档记录在 Windows 下从依赖下载到生成可执行文件的常用命令。

## 1. 环境要求

建议在 PowerShell 或 Windows Terminal 中执行。下面命令默认使用 PowerShell 写法。

```powershell
go version
node -v
npm -v
wails version
```

如果未安装 Wails CLI：

```powershell
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

确认 `%USERPROFILE%\go\bin` 已加入 `PATH`。

## 2. 进入项目根目录

```powershell
Set-Location D:\path\to\Ant-Browser
```

## 3. 下载 Go 依赖

国内网络建议先设置 Go 代理：

```powershell
go env -w GOPROXY=https://goproxy.cn,direct
```

下载依赖：

```powershell
go mod download
go mod tidy
```

## 4. 安装前端依赖

```powershell
cd frontend
npm install
npm run ensure:native
cd ..
```

如需使用代理：

```powershell
npm config set proxy http://127.0.0.1:7890
npm config set https-proxy http://127.0.0.1:7890
```

构建结束后可清理：

```powershell
npm config delete proxy
npm config delete https-proxy
```

## 5. 生成 Wails 绑定

```powershell
.\bat\generate-bindings.bat --no-pause
```

## 6. 构建前端资源

```powershell
cd frontend
npm run build:clean
cd ..
```

## 7. Go 编译检查

仅验证 Go 代码可编译：

```powershell
go test ./...
go build -o build\bin\ant-chrome.exe .
```

## 8. 正式构建桌面应用

推荐使用 Wails 构建：

```powershell
wails build
```

输出文件：

```text
build\bin\ant-chrome.exe
```

也可以使用项目内置脚本完成完整构建流程：

```powershell
$env:NO_PAUSE=1
.\bat\build.bat
```

该脚本会依次执行前端依赖安装、Go 依赖下载、Wails 绑定生成、前端构建、`wails build`，并复制 `bin\` 运行时依赖到 `build\bin\bin\`。

## 9. 开发运行

根目录提供了 Windows 入口：

```powershell
.\dev.bat
```

稳定模式：

```powershell
.\dev.bat stable
```

前端热更新模式：

```powershell
.\dev.bat live
```

低内存限制模式：

```powershell
.\dev.bat limited
```

不暂停退出：

```powershell
.\dev.bat stable --no-pause
```

## 10. 常用环境变量

```text
DEV_PROXY_URL   为 npm / Node / Go 下载流量注入 HTTP(S) 代理
DEV_NO_PROXY    设置 NO_PROXY / no_proxy
DEV_GOPROXY     覆盖 GOPROXY
NO_PAUSE=1      脚本结束后不 pause
CI=1            CI 环境下不 pause
```

分析当前项目，我想增加一个帐户管理功能，比如某个网站，我有多个帐户，但是不能使用同一个浏览器登陆。
也就是增加一个帐户管理器
功能1：增加站点
            每个站点可增加多个帐户，比如绑定的指纹浏览器id，用户名，密码，邮箱，邮箱密码，备注，操作【快速打开该网址】
            同一个指纹浏览器只能绑定同一个帐户。
功能2：自动阅读
        打开对应网站后，会自动模拟人工阅读。

功能3：可根据指纹浏览器筛选查看该指纹浏览器绑定了哪些帐户，然后操作帐户。

功能4：自动签到，
        有些网站带有签到功能，绑定的帐户里开启了自动签到功能，并绑定自动签到页面，执行自动签到后，会在后台打开不同浏览器的签到页面，自动识别页面中的
        签到按钮，进行签到操作。
        用户也可指定不同网站或不同程序的网站签到按钮。


要求1：自动签到功能如果一直切换浏览器，会不会导致速度满或卡顿，给出解决方案。

要求2：当前的代理ip功能是否能增加一个clash节点导入，然后把clash节点绑定到指纹浏览器的代理ip上，使用clash节点进行上网？
        这个功能比较复杂，考虑提取为一个单独的工作进行开发


